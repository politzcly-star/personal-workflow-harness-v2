# v6.2 本地升级完成记录

日期：2026-09-23。授权：升级工作流、就地替换近期项目中的旧工作流入口；不包含本轮提交、推送或业务部署。

## 已落地

- 日常 S1/S2 默认主智能体直接执行，以受影响检查和主智能体差异检查收尾；不强制规划、QA/验收双人组、全套测试或安全全面审计。
- 子智能体仅在独立工作可节省时间或隔离上下文时使用。配置偏好为 GPT-6 Sol medium 实施、GPT-6 Luna high 窄范围检查；必要的敏感边界独立复核使用 Sol high。保留当前主模型。
- 保留稳定角色 ID（含 terra_worker / luna_verifier）以兼容既有调用，但模型配置已更新，不表示沿用旧模型。
- 不支持角色/模型时，遵守同渠道、已授权、可用或继承模型的回退策略；不自动切换登录、供应商或凭据，不重复相同失败。必要独立复核缺失只阻塞相应边界。
- 工作流源仓库及 9 个近期相关目录完成就地更新；包括 task-home 明确指向的 r1 规范源码。sub2api 与 cursor助手根目录是新增安装，其他为替换。
- 从 4 个旧项目目录逐文件删除 27 个已核对通用旧入口，共 108 个文件。保留项目契约、业务规范、源码权威指引、历史任务与 Git 历史。旧交接文档保留历史正文并标为非当前入口。

## 验证与边界

- runtime 单元测试 24 项通过；迁移测试 34 项通过，共 58 项。未重复业务全套测试或扩大为全仓安全审计。
- 最终安装检查：9 个目录各 48 个受管文件与源文件字节及清单哈希一致；各有一个 v6.2 managed block；配置及静态 runtime 检查通过；108 个旧文件均已不存在。
- 源差异格式检查通过；8 个 Git 目标的受管文件及 AGENTS/CLAUDE 差异检查通过（允许 Windows CRLF 行尾）。未改写无关历史文件以统一格式。
- 独立复核覆盖源变更和前 8 个目录的入口/删除边界，无阻断问题；r1 由实施子智能体核对契约哈希、HEAD，再纳入主智能体最终安装检查。并未声称 r1 经过另一次独立审计。
- 来源 HEAD 为 3e414fb233e93a4b1d6a69abb471862a6220a49b，交付边界检查的源 origin/master 相同。未提交或推送本轮变更；保留已有未提交工作和缓存。
- 安装与静态测试不证明第三方代理已支持这些模型，也不证明实时子智能体选模、hook 调用或成本/耗时改善。回退是工作指引，不是新增自动供应商路由器；本轮未做第三方登录或付费对照实验。
- sub2api 现有 Git 忽略规则覆盖大部分工作流文件，因此该目标目前主要是本地安装；未擅自修改其忽略规则。cursor助手根目录不是 Git 仓库。
- 已打开任务可能仍持有旧上下文，继续任务时应重新读取当前 AGENTS.md；文件替换不等于刷新运行中的模型上下文。未发送消息自动恢复其他业务任务。

## 证据索引

最终安装状态以 `workflow-v62-installation-verification.json` 为准。删除清单为 `workflow-v62-removed-legacy.json`，已审遗留文件认领为 `workflow-v62-reviewed-legacy-adoption.json`。入口准备、事实指引和历史提示分别见同目录的 `workflow-v62-entry-replacement.json`、`workflow-v62-facts-entry-update.json`、`workflow-v62-historical-notice.json`。

注意：entry-replacement 中部分 after 哈希属于迁移前的中间阶段，不是最终安装快照。社区与官方调研建议保留在 `workflow-community-review-2026-09-23.md`。
