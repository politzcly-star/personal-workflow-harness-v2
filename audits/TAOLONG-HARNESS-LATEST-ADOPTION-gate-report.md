# TAOLONG-HARNESS-LATEST-ADOPTION Gate Report

Task ID: `TAOLONG-HARNESS-LATEST-ADOPTION`

Status: `CONDITIONAL GO RESOLVED - HARNESS CONFIGURATION COMMITTED AND PUSHED`

## Frozen Scope

- S-Level / route: `S3` / `review_gated` local configuration migration.
- Human authorization: accepted the existing `D:\taolong-ai-src` dirty baseline and approved Harness-only adoption in `D:\ai淘龙` and `D:\taolong-ai-src`.
- Forbidden boundaries remained closed: no business implementation edits, feature-worktree writes, secrets, server/production access, deployment, database action, staging, commit, push, model-value change, or global Codex change.

## Baseline Records

1. Harness repository
   - Root: `D:\个人工作流-v2`
   - Starting HEAD: `6e469b82f1cf4cb03d6f316debb5d55beeba3d53`
   - Starting status: only the frozen Task Packet was untracked.
2. Canonical source repository
   - Root / branch: `D:\taolong-ai-src` / `main`
   - HEAD: `71ee69495b074b526dbb7c70268ab941cac97770`
   - Schema-v2 baseline: `.codex/harness-state/TAOLONG-source-baseline.json`
   - Captured UTC: `2026-07-10T09:06:00.8180261Z`
   - Evidence: SHA-256 for 41 pre-existing dirty business paths and eight root/nested AGENTS paths.
3. Management root
   - Root: `D:\ai淘龙`; confirmed not a Git worktree.
   - Schema-v2 baseline: `.codex/harness-state/TAOLONG-management-baseline.json`
   - Captured UTC: `2026-07-10T09:06:01.0133476Z`
   - Evidence: hashes for the two existing allowed files and explicit nulls for the eight absent runtime assets.

The baseline files remained unchanged during implementation.

## Migration Repair And Fixtures

Changed `scripts/migrate-project-harness.ps1` so that:

- Git local-exclude discovery uses `git rev-parse --git-path info/exclude` and runs only for a real Git worktree;
- a non-Git target receives no `.git` directory or exclude file;
- a legacy file beginning with `# Production Harness Project Addendum` is recognized only when it also has the preservable `# Directory guide` boundary;
- replacement requires `-ReplaceLegacyAgents`, removes the legacy prefix, installs the current managed block, and preserves the complete Directory-guide tail apart from newline normalization;
- all AGENTS/template text is decoded as strict UTF-8 and every generated AGENTS file is written as BOM-less UTF-8 through `.NET UTF8Encoding(false, true)`, independent of Windows PowerShell's default encoding;
- Dry Run reports Git/non-Git behavior and exact Addendum preservation behavior.

Added deterministic self-test fixtures for a real temporary Git worktree, a non-Git project, and a legacy Addendum with an exact preserved tail. The Addendum fixture now contains U+2192, U+2014, and Chinese sentinels, decodes the generated bytes strictly, asserts zero BOM, and compares the decoded tail. Updated `docs/install-hooks-upgrade.md` with the compatibility and non-Git behavior.

An initial focused repair occurred: the first self-test found that piping native Git output through `Select-Object` changed the observed exit code to `-1`. The probe now captures native output before inspecting `$LASTEXITCODE`; the complete self-test then passed.

A subsequent root pre-Luna audit found a P1 encoding defect: Windows PowerShell's implicit AGENTS read/write encoding had added a UTF-8 BOM and converted existing arrows/em dashes to mojibake. The earlier preservation assertion was a false positive because both inputs were decoded through the same implicit encoding. Readiness was withdrawn. The migration path and fixture were changed to strict explicit UTF-8, the source AGENTS was restored byte-for-byte from the original local backup and remigrated, and the management AGENTS was remigrated to remove its BOM. Fresh raw-byte and Git-diff checks now pass.

## Changed Files

Harness repository:

- `scripts/migrate-project-harness.ps1`
- `scripts/harness-self-test.ps1`
- `docs/install-hooks-upgrade.md`
- `tasks/TAOLONG-HARNESS-LATEST-ADOPTION.md` (frozen input; unchanged after Human GO)
- `audits/TAOLONG-HARNESS-LATEST-ADOPTION-gate-report.md`

Management root:

- `AGENTS.md`
- `docs/project-profile.md` — execution/model/context notes only
- `.codex/config.toml`
- `.codex/agents/terra-worker.toml`
- `.codex/agents/luna-verifier.toml`
- `.codex/hooks.json`
- `.codex/hooks/harness-hook.ps1`
- `scripts/goal-state.ps1`
- `templates/goal-contract.md`
- `templates/context-capsule.md`

Canonical source root:

- `AGENTS.md`
- `docs/project-profile.md` — workflow/model/context fields only
- `.codex/config.toml`
- `.codex/agents/terra-worker.toml`
- `.codex/agents/luna-verifier.toml`
- `.codex/hooks.json`
- `.codex/hooks/harness-hook.ps1`
- `scripts/goal-state.ps1`
- `templates/goal-contract.md`
- `templates/context-capsule.md`
- local-only `.git/info/exclude` entries for `.codex/legacy-harness-backup/` and `.codex/harness-state/`
- local-only source AGENTS backups under `.codex/legacy-harness-backup/20260710-171054/` (original pre-migration bytes) and `.codex/legacy-harness-backup/20260710-172127/` (restored input before the corrected migration)

## Preservation And Configuration Evidence

- Management root still has no `.git` path after Apply and post-migration Dry Run.
- All eight installed runtime/Goal assets are byte-identical between the Harness source and both target roots.
- Installed JSON and PowerShell parse; installed TOML parses using Python `tomllib`.
- Routing remains exactly Terra Medium root, Terra High worker, and Luna Medium verifier.
- Both Hook JSON files contain `SessionStart`, `PreToolUse`, `PreCompact`, and `PostCompact`.
- Installed `SessionStart` resume fixtures returned bounded additional context in both roots.
- Strict UTF-8 raw-byte decoding proves both generated AGENTS files have no BOM and no mojibake/replacement marker.
- The source `# Directory guide` and every following rule compare character-for-character equal to the original raw migration backup after CRLF/LF and final-newline normalization; six U+2192 arrows and two U+2014 em dashes are preserved.
- Fresh `git diff --no-color -- AGENTS.md` shows only the legacy Harness prefix replacement plus final-newline normalization; it shows no encoding-only project-tail change.
- The source legacy Addendum heading is absent and exactly one managed Harness block exists.
- The management AGENTS file has exactly one current managed block and no old `Mandatory Start` workflow body.
- All seven nested source AGENTS files match the frozen SHA-256 baseline.
- All 41 pre-existing dirty business paths match the frozen SHA-256 baseline.
- Feature worktrees were only observed with read-only status: PPT had zero entries, dashboard eleven existing entries, document three existing entries, and `ai-refactor` 189 existing entries. The `ai-refactor` worktree remained at `71ee694` and its latest reflog/dirty-file timestamps predate the task baseline; no command wrote to any feature worktree.

## Checks Run

- `scripts/harness-self-test.ps1` — PASS after the Git-probe repair and again after the strict UTF-8 repair.
- `scripts/health-check.ps1 -Strict` — PASS.
- Management and source pre-migration Dry Runs — PASS with exact intended plans.
- Management and source post-migration Dry Runs — PASS with no blocker and all runtime assets current.
- Harness `git diff --check` — PASS.
- Source `git diff --check -- AGENTS.md docs/project-profile.md` — PASS.
- JSON/TOML/PowerShell parser checks — PASS.
- Cross-root runtime asset SHA-256 comparison — PASS.
- Baseline business/nested-AGENTS SHA-256 comparison — PASS.
- Legacy-prefix, managed-block-count, strict UTF-8/no-BOM/non-ASCII Directory-tail, required-Hook-event, and model-routing assertions — PASS.
- Source Git index inspection — PASS; cached path count is zero before Luna review.

Full `pnpm guard`, typecheck, package tests, builds, browser checks, server checks, and CodeGraph were not run because the module changes only repository workflow configuration and explicitly forbids business/runtime/server work. Compensation is exact hash preservation, parser checks, migration fixtures, installed Hook invocation, and exact scope comparison.

## Scope Guard

PASS for the implementation phase.

- Harness changes are an exact subset of the Packet allowlist.
- Source status is the accepted 41-path dirty baseline plus the approved Harness adoption paths; there is no unexpected status path.
- `.codex/**` is ignored by the source repository's existing tracked `.gitignore`; files are installed and hash-verified but do not appear in ordinary status. After Luna acceptance, branch finish must force-add only the five explicitly approved `.codex` files without changing `.gitignore`.
- Source index was limited to the approved ten Harness paths, then committed and pushed; the unrelated business baseline remains unstaged.

## Luna Decision

`CONDITIONAL GO`, resolved by adding the `ai-refactor` worktree evidence and correcting the PostCompact lifecycle documentation. No implementation or safety blocker remained.

## Residual Risk

- Project-scoped Hooks and model configuration require Human trust in each target before Codex relies on them.
- The source repository still contains the Human-accepted unrelated dirty business baseline; branch finish must use an exact path allowlist and inspect the staged diff.
- Existing source `.gitignore` ignores `.codex/`, so the authorized configuration files require exact-path force-add after Luna acceptance. No `.gitignore` change is needed or authorized.
- The preserved source AGENTS tail differs from its pre-task form only by removal of the final newline; the Task Packet explicitly permits unavoidable newline normalization.

## Commit And Push Result

Committed and pushed only the ten approved Harness configuration paths in `D:\taolong-ai-src`:

- Commit: `47705e9 chore: adopt autonomous goal harness`
- Remote: `origin/main` at `47705e907b505eb7d0362c9a9fb66bb4d89915ca`
- The accepted dirty business baseline remains unstaged; no business, worktree, server, or database changes were included.

## Next Recommended Action

Adoption is complete. Future changes should begin from the installed Goal Contract and preserve the same exact-path staging boundary.
