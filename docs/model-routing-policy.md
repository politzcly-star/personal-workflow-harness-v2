# Optional model roles, verified runtime
The selected root owns accepted delivery; don't pin or silently switch the root/provider. S-level is risk, not a model relay. Optional files in .codex/agents describe luna_qa (compact read-only QA), terra_worker (bounded writes), luna_verifier (independent final-boundary review), sol_planner (difficult design tradeoffs).
Use explicit user-approved roles where the actual runtime supports them. Otherwise follow the actual delegation API/model-selection permission and label inherited/configured/verified/unavailable accurately. A role file's presence, name or sandbox value cannot enforce an interface that does not consume it. Don't invent config registrations or claim routing from static validation. scripts/runtime-inspect.py is inventory only. Missing optional role support allows safe root work, but does not satisfy required independent review.
Scheduling benefits include parallel critical-path savings AND context isolation. Missing named-role/model switching support does not disable a supported, authorized inherited-model subagent. Start only useful children, usually one or two; reuse bounded subsystem context, avoid full-history forks. Root-direct remains appropriate for small work. Keep independent review and expose unavailable consent/support at start. No automatic dual QA/verifier pass on the same evidence.
Capacity max_threads=4/max_depth=1 is a ceiling, not a quota; actual tools may count root differently and stricter limits prevail. No automatic max/ultra escalation, paid probes or model identity retries. Count all exposed root/child usage; provider bills, not guessed list prices, decide monetary savings.
