# Project profile
Project:
Repository Root:
Native install / test / build / lint / run commands:
Frontend / backend / shared-contract ownership:
Generated paths / forbidden scope:
Isolated test data/ports/database and cleanup scope:
Compatibility / acceptance entry points:
Existing design tokens / representative UI:

## Execution
Root: inherit Human/platform selection
Delegation consent source / scope (copied file is not consent):
Runtime observations / source / client-version boundary (unknown stays unknown):
Effective context window / compaction threshold from runtime, if exposed:
Optional roles: .codex/agents/*.toml; verify adapter support. Missing named-role support does not disable supported, authorized inherited-model subagents. Root-direct only when suitable or delegation itself is unavailable.
Parallel write scope / shared integration owner:
Risk-proportional repair time/cost ceiling:

## Release configuration (NOT approval; no credentials)
Environment / configured alias / service:
Current-version query / immutable artifact ID or digest:
Build and preflight/config checks:
Release runner (pins artifact and serializes target):
Health + business smoke + expected version / timeout:
Abort signals / previous artifact / rollback command and checks:
Data migration impact / separate restore strategy:
Authorization source / target / version / UTC window / rollback envelope:
Approval transport: trusted platform/operator | externally signed Hook envelope | unavailable
Secret manager variable names only:
Private-browser state boundary:

## Baseline and reusable local evidence
Canonical source / thread-home distinction:
Required initial remote base / frozen task commit:
Push/integration boundary / project offline-start policy:
Local check IDs / commands / tool-config-fixture identity:
Evidence paths / invalidation conditions (external state needs fresh checks):
